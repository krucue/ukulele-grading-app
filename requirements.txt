# Core logic (similarity.py, scorer.py, config_loader.py, pipeline.py)
# ไม่มี dependency ภายนอก — รัน demo/test ได้ทันทีโดยไม่ต้องติดตั้งอะไรในไฟล์นี้เลย

# เว็บแอป (web_app.py + webapp/) — ครูเปิดใช้งานผ่านเบราว์เซอร์ ไม่ต้องแตะ terminal
flask>=3.0.0

# แตกหน้าจากไฟล์ PDF ที่สแกนมา (pdf_pages.py) — เครื่องสแกนกับแอปสแกนบนมือถือ
# คายไฟล์ออกมาเป็น PDF ไฟล์เดียวจบ ไม่ใช่ .jpg แยกหน้า
# เลือก pypdf เพราะเป็น pure-python สัญญาอนุญาต BSD ไม่ต้องลงไบนารีนอก pip
# (pymupdf เร็วกว่าแต่เป็น AGPL, pdf2image ต้องลง poppler แยกเอง)
pypdf>=5.0.0

# ขั้นตอนปรับแนว/ตัดภาพ (align.py, regions.py) และเครื่องมือ calibrate
opencv-python-headless>=4.9.0
numpy>=1.26.0
pillow>=10.0.0

# โหมดตรวจจริงทั้งสาย: อ่านลายมือจากรูป (ocr.py) + ตรวจข้อบรรยาย (llm_grader.py)
# ทั้งสองอย่างใช้คีย์ Anthropic ใบเดียวกัน ไม่ต้องมีบัญชีคลาวด์อื่นเลย
anthropic>=0.40.0

# ของ Google (Sheets / Vision) ไม่อยู่ในไฟล์นี้ ไม่ต้องติดตั้งก็ตรวจข้อสอบได้ครบ
# ถ้าจะบันทึกลง Google Sheets แทน CSV: pip install -r requirements-google.txt

# QR code ในหน้าต่างดำ ตอนเปิดโหมดมือถือ — ครูสแกนแทนการพิมพ์ IP เอง
# pure-python ไม่ต้องลงไบนารีอะไรเพิ่ม และถ้าไม่มีตัวนี้โปรแกรมก็ยังเปิดได้ปกติ
# แค่ต้องพิมพ์ URL เอง (ดู print_qr_code() ใน web_app.py)
qrcode>=7.4

# อ่านรูป .heic ของ iPhone (heic.py) — iPhone ตั้งค่ากล้องมาจากโรงงานเป็น High
# Efficiency ซึ่งเซฟเป็น .heic ที่ opencv อ่านไม่ได้ ตัวนี้แปลงเป็น .jpg ให้ก่อน
# ถ้าไม่ติดตั้ง โปรแกรมยังใช้ได้ครบทุกอย่าง แค่รับ .heic ไม่ได้และจะบอกวิธีแก้ให้
pillow-heif>=0.16
